{
  "name": "@scrowl/root",
  "private": true,
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "lint": "",
    "build:player": "yarn workspace @scrowl/player run build",
    "build:runtime": "yarn workspace @scrowl/runtime run build",
    "update": "yarn build:runtime && yarn build:player && yarn workspace @scrowl/editor run copy:assets",
    "start": "yarn workspace @scrowl/editor run start",
    "build": ""
  },
  "workspaces": [
    "config",
    "packages/*",
    "packages/templates/*"
  ],
  "devDependencies": {
    "lerna": "^5.5.4"
  }
}
