{
  "packages": [
    "config",
    "packages/*",
    "plugins/*"
  ],
  "npmClient": "yarn",
  "useWorkspaces": true,
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": ["main"],
      "conventionalCommits": true
    },
    "publish": {
      "ignoreChanges": [
        "*.md",
        "*.test.*"
      ]
    }
  }
}
